# E2E 스모크 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** jsdom이 구조적으로 못 잡는 렌더 회귀(표 격자 실종·툴바 세로 붕괴·핸들 크래시)를 실제
브라우저에서 자동 검출하는 Playwright 스모크 5건과, 그것이 붙을 프로덕션 차단 하니스를 만든다.

**Architecture:** `PostEditor`는 props 3개(`initialDoc`/`placeholder`/`onDocChange`)뿐이라 Supabase·
auth·라우터 의존이 0이다. 그래서 `(app)` 라우트 그룹 **바깥**에 하니스 라우트를 하나 두고 실제 글
상세와 같은 `.detail-page` 자식 구성을 재현해 마운트한다. 프로덕션 유출은 2겹으로 막는다 — 빌드
타임(`pageExtensions`로 `page.e2e.tsx`를 아예 제외)과 런타임(`notFound()`).

> **정정(2026-07-30, 최종 브랜치 리뷰):** 위 "2겹" 서술 중 런타임 겹은 계획대로 동작하지
> 않았다 — `NEXT_PUBLIC_E2E`는 빌드 타임 인라인이라 플래그를 켠 빌드에서는 런타임 검사가
> 죽은 코드였다. 실제 수정과 사실관계 전문은
> `docs/superpowers/specs/2026-07-27-e2e-smoke-design.md` §4.2 뒤의 정정 각주를 볼 것.
> 요약: `page.e2e.tsx`에 이 플래그와 무관한 `NODE_ENV === "production"` 검사를 더해 실제
> 독립된 2겹으로 만들었다. 이 문서의 나머지 서술(코드 스니펫 포함)은 당시 기록 그대로 둔다.

**Tech Stack:** Next.js 16.2.10 (App Router, Turbopack) · React 19 · Tiptap 3.28 · vitest 4.1.10 (jsdom) ·
**@playwright/test 1.62.0** (신규, Apache-2.0, devDependency) · Node 24.18.0

**스펙:** `docs/superpowers/specs/2026-07-27-e2e-smoke-design.md` — 이 계획은 그 스펙의 구현이다.
스펙과 어긋나는 결정을 내리면 스펙을 함께 고친다.

## Global Constraints

- **패키지 매니저: npm.** `package-lock.json`이 단일 원본이다.
- **Playwright는 chromium 단일.** 다중 브라우저·모바일 뷰포트는 비목표(스펙 §7).
- **픽셀 스크린샷 베이스라인 금지.** macOS↔Linux 렌더 차이로 상시 flaky → CI를 다시 무시하게 된다(스펙 §7).
- **`retries: 0`.** flaky를 재시도로 감추지 않는다. 이 저장소는 "CI 빨간불 = 진짜 신호"를 막 회복했다(`09624d6`).
- **마커 상수는 `__MN_E2E_HARNESS__`**, 플래그는 **`NEXT_PUBLIC_E2E`**, 값 **`"1"`**일 때만 활성.
- **하니스 라우트 경로: `/e2e-harness/editor`.** 밑줄 접두사 폴더(`_foo`)는 Next.js가 라우팅에서 제외하므로 절대 쓰지 않는다.
- **`package.json`의 `engines.node`는 건드리지 않는다.** Vercel이 빌드 Node 선택에 그 값을 쓴다.
- **한국어 주석 + conventional prefix 커밋**, 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.
- **커밋마다 단독으로 `npm test && npm run build` 통과**(`.claude/skills/commit-hygiene`).
- 검증 기준선: 현재 **64파일 / 520 테스트**. 태스크마다 이 수가 어떻게 늘어나는지 명시한다.

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `components/e2e/EditorHarness.tsx` | 하니스 본체(클라이언트). `.detail-page` 자식 구성 재현 + 마커 보유 | 1 |
| `app/e2e-harness/editor/page.e2e.tsx` | 라우트 진입점(서버). 런타임 플래그 게이트 + `notFound()` | 1 |
| `next.config.ts` | 빌드 타임 게이트(`pageExtensions`) | 1 |
| `__tests__/e2e-harness-gate.test.tsx` | 게이트 유닛 검증 | 1 |
| `scripts/verify-harness-excluded.mjs` | 번들 유출 검증(마커 부재 + 라우트 부재) | 2 |
| `playwright.config.ts` | Playwright 설정 + `webServer` | 3 |
| `e2e/fixtures.ts` | `pageerror` 전역 그물 + `openHarness` 헬퍼 | 3 |
| `e2e/harness.spec.ts` | 하니스 자체 검증(자식 구성 7종) | 3 |
| `e2e/table-grid.spec.ts` | 스모크 #1 표 격자 | 4 |
| `e2e/toolbar-layout.spec.ts` | 스모크 #2 툴바 한 줄 | 5 |
| `e2e/handle-menu.spec.ts` | 스모크 #3 핸들 메뉴 | 6 |
| `e2e/color-popover.spec.ts` | 스모크 #4 색 팝오버 | 7 |
| `e2e/table-toolbar.spec.ts` | 스모크 #5 표 플로팅 툴바 | 8 |
| `vitest.config.ts` | `e2e/**` 제외(스펙 파일 이중 수집 차단) | 3 |
| `.github/workflows/ci.yml` | CI 편입 | 9 |

---

### Task 1: 하니스 라우트 + 2겹 프로덕션 차단

**Files:**
- Create: `components/e2e/EditorHarness.tsx`
- Create: `app/e2e-harness/editor/page.e2e.tsx`
- Create: `__tests__/e2e-harness-gate.test.tsx`
- Modify: `next.config.ts`

**Interfaces:**
- Produces: `EditorHarness()` — 인자 없는 named export. `/e2e-harness/editor`에서 렌더되며
  `.detail-page[data-harness="__MN_E2E_HARNESS__"]` 래퍼 아래 §4.1.1의 자식 7종을 그린다.
  이후 모든 E2E 태스크가 이 DOM에 의존한다.
- Produces: `page.e2e.tsx`의 `default export Page()` — 인자 없음, 플래그 없으면 `notFound()`.

**배경(왜 이 구조인가):** 서버 컴포넌트에서 클라이언트 컴포넌트로 함수 prop을 넘기면 빌드가
깨진다(`Error: Event handlers cannot be passed to Client Component props` — 실측 확인). 그래서
페이지(서버)는 게이트만 하고 props 없이 `<EditorHarness />`만 그리며, no-op 핸들러는 하니스
내부에 둔다.

- [ ] **Step 1: 게이트 유닛 테스트를 먼저 쓴다**

Create `__tests__/e2e-harness-gate.test.tsx`:

```tsx
import { afterEach, expect, test, vi } from "vitest";

/**
 * 하니스 라우트의 런타임 차단 겹 검증.
 *
 * 차단은 2겹이다 — 빌드 타임(next.config.ts의 pageExtensions가 page.e2e.tsx를 아예
 * 제외)과 런타임(여기서 보는 notFound()). 빌드 타임 겹은 유닛 테스트로 볼 수 없으므로
 * scripts/verify-harness-excluded.mjs가 담당한다.
 */
const notFoundMock = vi.hoisted(() => vi.fn());
vi.mock("next/navigation", () => ({ notFound: notFoundMock }));

// 페이지가 process.env를 함수 본문에서 읽으므로 모듈 재import 없이 stub만으로 충분하다.
import Page from "@/app/e2e-harness/editor/page.e2e";

afterEach(() => {
  notFoundMock.mockClear();
  vi.unstubAllEnvs();
});

test("NEXT_PUBLIC_E2E가 없으면 notFound()로 차단한다", () => {
  vi.stubEnv("NEXT_PUBLIC_E2E", "");
  Page();
  expect(notFoundMock).toHaveBeenCalledTimes(1);
});

test("NEXT_PUBLIC_E2E가 '1'이 아닌 값이어도 차단한다", () => {
  vi.stubEnv("NEXT_PUBLIC_E2E", "true");
  Page();
  expect(notFoundMock).toHaveBeenCalledTimes(1);
});

test("NEXT_PUBLIC_E2E가 '1'이면 통과한다", () => {
  vi.stubEnv("NEXT_PUBLIC_E2E", "1");
  Page();
  expect(notFoundMock).not.toHaveBeenCalled();
});
```

- [ ] **Step 2: 실패를 확인한다**

Run: `npx vitest run __tests__/e2e-harness-gate.test.tsx`
Expected: FAIL — `Failed to resolve import "@/app/e2e-harness/editor/page.e2e"`

- [ ] **Step 3: 하니스 본체를 만든다**

Create `components/e2e/EditorHarness.tsx`:

```tsx
"use client";

import { ArrowLeft, Calendar, ChevronRight, Trash2 } from "lucide-react";
import { CharCount } from "@/components/CharCount";
import { PostEditor } from "@/components/editor/PostEditor";
import { PageIconButton } from "@/components/icon/PageIconButton";
import { IconButton } from "@/components/ui/IconButton";

/**
 * E2E 스모크 전용 하니스. 프로덕션 번들에는 들어가지 않는다
 * (next.config.ts의 pageExtensions가 진입 페이지를 빌드에서 제외 → 이 모듈은 importer가 없다).
 *
 * 원칙(스펙 §4.1.1): **하니스는 .detail-page의 실제 자식 구성을 따른다.**
 * 형제 요소를 생략하면 레이아웃 컨텍스트 버그의 사각지대가 생긴다 —
 * .top-toolbar-sticky는 `position: sticky; top: 0`(globals.css:1095)이라
 * 앞선 형제들의 높이가 스티키 동작에 직접 관여하고, .detail-cover 하나만 해도 200px다.
 *
 * 스토어·라우터·네트워크에 의존하는 자식은 **같은 클래스의 정적 대역**으로 둔다.
 * 레이아웃에 기여하는 것은 박스이지 동작이 아니고, 하니스의 "의존 0" 성질을 잃으면 안 된다.
 */

/** 번들 유출 검증(scripts/verify-harness-excluded.mjs)이 찾는 마커. */
const HARNESS_MARKER = "__MN_E2E_HARNESS__";

/** 날짜·본문은 고정값이다. 실시간 값은 스냅샷을 비결정적으로 만든다. */
const FIXED_TITLE = "하니스 문서";
const FIXED_DATE = "7월 29일";
const FIXED_TEXT = "하니스 본문";

const noop = () => {};

export function EditorHarness() {
  return (
    <div className="detail-page" data-harness={HARNESS_MARKER}>
      {/* 1. 브레드크럼 — 정적 대역(실제는 router + app.posts 조상 체인 의존) */}
      <div className="detail-breadcrumb">
        <IconButton icon={ArrowLeft} title="뒤로" onClick={noop} />
        <button type="button" className="detail-breadcrumb__root" onClick={noop}>
          내 업무
        </button>
        <span className="detail-breadcrumb__sep">
          <ChevronRight size={14} />
        </span>
        <span className="detail-breadcrumb__current">{FIXED_TITLE}</span>
        <div className="detail-breadcrumb__spacer" />
        <button type="button" className="detail-delete-btn" onClick={noop}>
          <Trash2 size={16} />
          삭제
        </button>
      </div>

      {/* 2. 커버 — 정적 대역. 실제 PostCover는 외부 이미지를 fetch하므로 그대로 쓰면
          네트워크가 flaky 요인이 된다. 박스(height 200px)는 CSS에서 동일하게 나온다. */}
      <div className="detail-cover" aria-hidden="true" />

      {/* 3. 페이지 아이콘 — props 전용이라 실컴포넌트 그대로 */}
      <PageIconButton icon={null} onChange={noop} />

      {/* 4. 제목 — 실마크업. 제어 input이므로 no-op onChange로 React 경고를 막는다. */}
      <input
        className="detail-title"
        value={FIXED_TITLE}
        onChange={noop}
        placeholder="제목 없음"
      />

      {/* 5. 메타 — 실마크업, 날짜만 고정 */}
      <div className="detail-meta">
        <Calendar size={14} />
        <span>{FIXED_DATE} 작성</span>
        <span className="detail-meta__dot" />
        <span>자동 저장됨</span>
      </div>

      {/* 6. 에디터 — 스모크의 주 대상 */}
      <PostEditor
        initialDoc={{ type: "doc", content: [] }}
        placeholder="내용을 입력하세요. 떠오르는 생각, 할 일, 메모를 자유롭게 기록해 보세요."
        onDocChange={noop}
      />

      {/* 7. 글자 수 — props 전용이라 실컴포넌트 그대로 */}
      <CharCount text={FIXED_TEXT} />
    </div>
  );
}
```

- [ ] **Step 4: 라우트 진입점을 만든다**

Create `app/e2e-harness/editor/page.e2e.tsx`:

```tsx
import { notFound } from "next/navigation";
import { EditorHarness } from "@/components/e2e/EditorHarness";

/**
 * E2E 하니스 라우트 — /e2e-harness/editor
 *
 * 폴더 이름에 밑줄을 쓰지 않는다. `_foo`는 Next.js의 private folder라
 * 폴더와 모든 하위가 라우팅에서 제외된다
 * (node_modules/next/dist/docs/01-app/01-getting-started/02-project-structure.md:257-261).
 *
 * 이 파일은 서버 컴포넌트다. 클라이언트 컴포넌트로 두고 함수 prop을 넘기면
 * 빌드가 깨지므로(Event handlers cannot be passed to Client Component props),
 * no-op 핸들러는 EditorHarness 내부에 둔다.
 *
 * 차단 2겹 중 **런타임 겹**. 빌드 타임 겹은 next.config.ts의 pageExtensions다.
 * 플래그를 켠 채 만든 빌드가 어쩌다 배포되는 경우를 여기서 막는다.
 */
export default function Page() {
  if (process.env.NEXT_PUBLIC_E2E !== "1") notFound();
  return <EditorHarness />;
}
```

- [ ] **Step 5: 빌드 타임 게이트를 넣는다**

Modify `next.config.ts` — 파일 전체를 아래로 교체:

```ts
import type { NextConfig } from "next";

// E2E 하니스 노출 여부. 빌드 타임 차단 겹이다 —
// 이 값이 "1"이 아니면 page.e2e.tsx가 페이지로 인식되지 않아 라우트도, 클라이언트 청크도
// 만들어지지 않는다. 런타임 notFound()만으로는 클라이언트 청크가 빌드 그래프에 남아
// 마커가 .next/static으로 유출된다(실측 확인).
const e2e = process.env.NEXT_PUBLIC_E2E === "1";

const nextConfig: NextConfig = {
  // Vercel 배포용 설정
  // GitHub Pages용 static export와 basePath를 제거했습니다.
  images: {
    unoptimized: true,
  },
  // 기본값(tsx/ts/jsx/js)에 하니스 전용 확장자를 조건부로 더한다.
  pageExtensions: ["tsx", "ts", "jsx", "js", ...(e2e ? ["e2e.tsx"] : [])],
};

export default nextConfig;
```

- [ ] **Step 6: 유닛 테스트 통과를 확인한다**

Run: `npx vitest run __tests__/e2e-harness-gate.test.tsx`
Expected: PASS — 3 tests

- [ ] **Step 7: 양방향 빌드로 2겹을 눈으로 확인한다**

```bash
rm -rf .next && NEXT_PUBLIC_E2E=1 npm run build 2>&1 | grep e2e-harness
```
Expected: `├ ○ /e2e-harness/editor` 가 라우트 목록에 나타난다

```bash
rm -rf .next && npm run build 2>&1 | grep e2e-harness; echo "exit=$?"
grep -rl "__MN_E2E_HARNESS__" .next/static | head
```
Expected: 라우트 목록에 없음(grep 무출력), 마커 grep 무출력

- [ ] **Step 8: 전체 게이트 후 커밋**

```bash
npm test && npm run build
```
Expected: **65 파일 / 523 테스트** 통과, 빌드 성공

```bash
git add components/e2e/EditorHarness.tsx app/e2e-harness/editor/page.e2e.tsx \
        __tests__/e2e-harness-gate.test.tsx next.config.ts
git commit -m "feat: E2E 하니스 라우트 + 빌드/런타임 2겹 프로덕션 차단

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 번들 유출 검증 스크립트

**Files:**
- Create: `scripts/verify-harness-excluded.mjs`
- Modify: `package.json` (scripts에 `verify:harness` 추가)

**Interfaces:**
- Consumes: Task 1의 마커 `__MN_E2E_HARNESS__`와 라우트 `/e2e-harness/editor`.
- Produces: `npm run verify:harness` — 통과 시 exit 0, 유출 시 exit 1. Task 9가 CI에서 호출한다.

**왜 스크립트인가:** 유닛 테스트는 런타임 겹만 본다. 빌드 타임 겹은 "빌드 산출물에 무엇이
없는가"라서 vitest로 볼 수 없다. 그리고 **로컬에서 재현 가능해야 한다** — CI에서만 도는 검증은
결국 아무도 안 돌린다.

- [ ] **Step 1: 스크립트를 쓴다**

Create `scripts/verify-harness-excluded.mjs`:

```js
#!/usr/bin/env node
/**
 * E2E 하니스가 프로덕션 빌드에서 완전히 빠졌는지 검증한다.
 *
 * 두 가지를 본다:
 *   (1) 라우트 부재 — 빌드 출력의 라우트 목록에 /e2e-harness/editor 가 없다
 *   (2) 마커 부재 — .next/static 어디에도 __MN_E2E_HARNESS__ 가 없다
 *
 * (2)가 핵심이다. 런타임 notFound()만으로는 클라이언트 청크가 빌드 그래프에 남아
 * 마커가 유출된다. 빌드 타임 게이트(next.config.ts의 pageExtensions)가 실제로
 * 동작하는지를 이 검사가 증명한다.
 */
import { execFileSync } from "node:child_process";
import { readdirSync, readFileSync, rmSync } from "node:fs";
import path from "node:path";

const MARKER = "__MN_E2E_HARNESS__";
const ROUTE = "/e2e-harness/editor";
const STATIC_DIR = ".next/static";

// 플래그를 확실히 제거한 환경에서 빌드한다(호출자 셸에 남아 있을 수 있다).
const env = { ...process.env };
delete env.NEXT_PUBLIC_E2E;

rmSync(".next", { recursive: true, force: true });

let buildOut;
try {
  buildOut = execFileSync("npm", ["run", "build"], { encoding: "utf8", env });
} catch (err) {
  console.error("✗ 빌드 실패 — 검증 불가");
  console.error(err.stdout ?? err.message);
  process.exit(1);
}

const failures = [];

if (buildOut.includes(ROUTE)) {
  failures.push(`라우트 ${ROUTE} 가 빌드 산출물에 존재한다`);
}

/** .next/static 전체를 훑어 마커를 포함한 파일을 모은다. */
function findMarker(dir) {
  const hits = [];
  for (const entry of readdirSync(dir, { withFileTypes: true })) {
    const full = path.join(dir, entry.name);
    if (entry.isDirectory()) {
      hits.push(...findMarker(full));
    } else {
      // 바이너리를 utf8로 읽어도 부분 문자열 탐색에는 문제가 없다.
      if (readFileSync(full, "utf8").includes(MARKER)) hits.push(full);
    }
  }
  return hits;
}

const leaked = findMarker(STATIC_DIR);
if (leaked.length > 0) {
  failures.push(`마커 ${MARKER} 유출 (${leaked.length}건):\n    ${leaked.join("\n    ")}`);
}

if (failures.length > 0) {
  console.error("✗ 하니스가 프로덕션 빌드에 유출됐다:");
  for (const f of failures) console.error(`  - ${f}`);
  process.exit(1);
}

console.log(`✓ 하니스 미유출 확인 — 라우트 부재 + ${STATIC_DIR} 마커 0건`);
```

- [ ] **Step 2: package.json에 스크립트를 추가한다**

Modify `package.json` — `scripts` 블록:

```json
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:harness": "node scripts/verify-harness-excluded.mjs"
  },
```

- [ ] **Step 3: 통과를 확인한다**

Run: `npm run verify:harness`
Expected: `✓ 하니스 미유출 확인 — 라우트 부재 + .next/static 마커 0건`, exit 0

- [ ] **Step 4: 음성 대조 — 이 검사가 실패할 수 있음을 증명한다**

실패하지 못하는 검사는 아무것도 지키지 못한다. `next.config.ts`의 `pageExtensions` 줄을
잠시 무조건 포함으로 바꾼다:

```ts
  pageExtensions: ["tsx", "ts", "jsx", "js", "e2e.tsx"],
```

Run: `npm run verify:harness`
Expected: **FAIL** — `✗ 하니스가 프로덕션 빌드에 유출됐다` + 라우트·마커 양쪽 보고, exit 1

확인했으면 Step 5의 원래 줄로 **반드시 되돌린다**:

```ts
  pageExtensions: ["tsx", "ts", "jsx", "js", ...(e2e ? ["e2e.tsx"] : [])],
```

Run: `npm run verify:harness` → 다시 exit 0 인지 확인

- [ ] **Step 5: 게이트 후 커밋**

```bash
npm test && npm run build
```
Expected: 65파일 / 523 테스트 통과, 빌드 성공

```bash
git add scripts/verify-harness-excluded.mjs package.json
git commit -m "test: 하니스 번들 유출 검증 스크립트 — 라우트·마커 부재 확인

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: Playwright 도입 + vitest 격리 + pageerror 전역 그물

**Files:**
- Create: `playwright.config.ts`
- Create: `e2e/fixtures.ts`
- Create: `e2e/harness.spec.ts`
- Modify: `vitest.config.ts`
- Modify: `package.json` (devDependency + `test:e2e`)
- Modify: `.gitignore`

**Interfaces:**
- Produces: `e2e/fixtures.ts`가 `test`(자동 pageerror 그물 포함)와 `expect`를 re-export하고,
  `openHarness(page: Page): Promise<void>` 를 제공한다. Task 4~8은 전부
  `import { test, expect, openHarness } from "./fixtures"` 로 시작한다.

**함정(반드시 처리):** vitest 기본 `include`는 `**/*.{test,spec}.?(c|m)[jt]s?(x)`라
**`e2e/*.spec.ts`를 그대로 수집한다.** 그러면 `npm test`가 Playwright 스펙을 실행하려 들어
깨진다. `vitest.config.ts`의 `exclude`에 `e2e/**`를 더해 격리한다.

- [ ] **Step 1: Playwright를 설치한다**

```bash
npm install -D @playwright/test@1.62.0
npx playwright install --with-deps chromium
```

라이선스 확인(이 저장소 관례):
```bash
npm view @playwright/test license
```
Expected: `Apache-2.0` — 허용형 라이선스, devDependency 전용이라 배포 산출물에 포함되지 않는다.

- [ ] **Step 2: vitest에서 e2e를 격리한다**

Modify `vitest.config.ts` — `exclude` 줄을 다음으로 교체:

```ts
    // 이 저장소는 .claude/worktrees/ 아래에 워크트리(체크아웃 사본)를 둔다.
    // 기본 include 글롭이 그 안의 __tests__ 까지 긁어 같은 테스트가 사본 수만큼
    // 중복 수집된다(메인 20 → 51파일). 사본이 초록이면 본체가 깨져도 총합이
    // 초록으로 보여 머지 검증 신호가 망가지므로 제외한다.
    //
    // e2e/** 도 제외한다. vitest 기본 include 는 **/*.spec.ts 를 수집하므로
    // Playwright 스펙을 jsdom 에서 실행하려 들어 npm test 가 깨진다.
    // configDefaults.exclude 를 펼쳐 기본값(node_modules/.git)을 잃지 않게 한다.
    exclude: [...configDefaults.exclude, '**/.claude/worktrees/**', 'e2e/**'],
```

- [ ] **Step 3: Playwright 설정을 만든다**

Create `playwright.config.ts`:

```ts
import { defineConfig, devices } from "@playwright/test";

/**
 * E2E 스모크 설정. chromium 단일 — 다중 브라우저·모바일 뷰포트는 비목표(스펙 §7).
 *
 * retries는 0이다. 재시도는 flaky를 초록으로 덮어 "CI 빨간불 = 진짜 신호"를 다시
 * 망가뜨린다. 이 저장소는 그 병(main push 5회 연속 실패 방치)을 09624d6에서 막 고쳤다.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? "line" : "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "retain-on-failure",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:3000",
    // 하니스 라우트는 이 플래그가 "1"일 때만 존재한다(빌드/런타임 2겹 게이트).
    env: { NEXT_PUBLIC_E2E: "1" },
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
```

- [ ] **Step 4: pageerror 전역 그물과 진입 헬퍼를 만든다**

Create `e2e/fixtures.ts`:

```ts
import { test as base, expect, type Page } from "@playwright/test";

/**
 * 전역 그물 — 모든 테스트에 자동 적용되는 pageerror 수집기.
 *
 * `auto: true`라 테스트가 이 픽스처를 요청하지 않아도 켜진다. 테스트 본문이 끝난 뒤
 * 미처리 예외가 1건이라도 있으면 실패시킨다. 2026-07-23 lockDragHandle 크래시처럼
 * "화면은 그려졌는데 콘솔에서 죽는" 회귀를 잡는 장치다.
 */
export const test = base.extend<{ pageErrors: Error[] }>({
  pageErrors: [
    async ({ page }, use) => {
      const errors: Error[] = [];
      page.on("pageerror", (err) => errors.push(err));
      await use(errors);
      expect(
        errors.map((e) => e.message),
        "페이지에서 미처리 예외가 발생했다"
      ).toEqual([]);
    },
    { auto: true },
  ],
});

export { expect };

/** 하니스를 열고 에디터가 마운트될 때까지 기다린다. 모든 스모크의 시작점. */
export async function openHarness(page: Page): Promise<void> {
  await page.goto("/e2e-harness/editor");
  await page.locator(".detail-content").waitFor({ state: "visible" });
}
```

- [ ] **Step 5: 하니스 자체를 검증하는 첫 스펙을 쓴다**

Create `e2e/harness.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 하니스가 스펙 §4.1.1 원칙("`.detail-page`의 실제 자식 구성을 따른다")을 지키는지 본다.
 * 형제 하나가 조용히 빠지면 레이아웃 컨텍스트가 달라져 나머지 스모크가 거짓 안심을 준다 —
 * 그래서 하니스 자신을 먼저 지킨다.
 */
test("하니스가 뜨고 .detail-page 자식 7종이 모두 있다", async ({ page }) => {
  await openHarness(page);

  const root = page.locator(".detail-page[data-harness]");
  await expect(root).toBeVisible();

  await expect(root.locator(".detail-breadcrumb")).toBeVisible();
  await expect(root.locator(".detail-cover")).toBeAttached();
  await expect(root.locator(".detail-title")).toBeVisible();
  await expect(root.locator(".detail-meta")).toBeVisible();
  await expect(root.locator(".detail-content")).toBeVisible();
  await expect(root.locator(".top-toolbar")).toBeVisible();
});

test("에디터에 입력이 들어간다", async ({ page }) => {
  await openHarness(page);
  await page.locator(".detail-content").click();
  await page.keyboard.type("하니스 입력 확인");
  await expect(page.locator(".detail-content")).toContainText("하니스 입력 확인");
});
```

- [ ] **Step 6: package.json에 스크립트를 추가한다**

Modify `package.json` — `scripts`에 한 줄 추가:

```json
    "test:e2e": "playwright test",
```

- [ ] **Step 7: .gitignore에 산출물을 추가한다**

Modify `.gitignore` — 파일 끝에 추가:

```
# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
```

- [ ] **Step 8: E2E가 도는지 확인한다**

Run: `npm run test:e2e`
Expected: **2 passed** (chromium)

- [ ] **Step 9: vitest가 e2e를 집어삼키지 않는지 확인한다**

Run: `npm test`
Expected: **65 파일 / 523 테스트** — 파일 수가 65 그대로여야 한다(e2e 스펙 2건이 섞이면 67로 늘거나 실패한다)

- [ ] **Step 10: 게이트 후 커밋**

```bash
npm test && npm run build
```

```bash
git add playwright.config.ts e2e/ vitest.config.ts package.json package-lock.json .gitignore
git commit -m "test: Playwright 스모크 기반 — pageerror 전역 그물 + 하니스 진입 검증

vitest 기본 include가 e2e/*.spec.ts를 수집해 npm test를 깨뜨리므로 exclude에 e2e/** 추가.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 스모크 #1 — 표 격자 (2026-07-27 회귀 직격)

**Files:**
- Create: `e2e/table-grid.spec.ts`

**Interfaces:**
- Consumes: `openHarness`, `test`, `expect` (Task 3).

**단언값의 출처(추측 금지):**
`globals.css:1058` → `.tbl td, .tbl th { border: 1px solid var(--border-default); height: 34px; }`
`--border-default` → `--gray-200` → `#e2e3e5` = `rgb(226, 227, 229)` (globals.css:14, 60)
`.tbl th { background: var(--surface-subtle); }` → `--gray-50` → `#f7f8f9` = `rgb(247, 248, 249)` (globals.css:11, 53)
표 삽입 기본값은 `{ rows: 3, cols: 3, withHeaderRow: true }` (TopToolbar.tsx:347).

- [ ] **Step 1: 테스트를 쓴다**

Create `e2e/table-grid.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 2026-07-27 회귀 직격 — 표 격자 실종.
 *
 * resizable:true인 표는 renderHTML이 아니라 nodeView(TableView)를 타는데,
 * prosemirror-tables의 columnResizing이 nodeView를 만들 때 HTMLAttributes를 넘기지 않아
 * 라이브 <table>에서 class="tbl"이 통째로 사라졌다. 직렬화(getHTML())에는 붙어 있어서
 * 기존 테스트 515개가 전부 초록인 채 화면만 깨져 있었다.
 *
 * jsdom은 var()를 해석하지 못해 실제 색까지는 볼 수 없다. 여기서는 진짜 브라우저가
 * 계산한 값을 본다.
 */
test("표를 삽입하면 격자 border가 실제로 계산된다", async ({ page }) => {
  await openHarness(page);
  await page.locator(".detail-content").click();
  await page.locator(".top-toolbar").getByRole("button", { name: "표 삽입" }).click();

  const table = page.locator(".detail-content table");
  await expect(table).toBeVisible();

  // 회귀 지점 그 자체 — 라이브 DOM에 클래스가 붙는가.
  await expect(table).toHaveClass(/\btbl\b/);

  // 3x3 + 헤더 행(TopToolbar.tsx:347)
  await expect(table.locator("th")).toHaveCount(3);
  await expect(table.locator("td")).toHaveCount(6);

  const td = table.locator("td").first();
  await expect(td).toHaveCSS("border-top-width", "1px");
  await expect(td).toHaveCSS("border-top-style", "solid");
  await expect(td).toHaveCSS("border-top-color", "rgb(226, 227, 229)");

  // globals.css:1058 의 height: 34px. 내용이 늘면 커질 수 있으므로 하한으로 본다.
  const box = await td.boundingBox();
  expect(box).not.toBeNull();
  expect(box!.height).toBeGreaterThanOrEqual(34);

  // 헤더 배경(--surface-subtle → --gray-50 → #f7f8f9)
  await expect(table.locator("th").first()).toHaveCSS(
    "background-color",
    "rgb(247, 248, 249)"
  );
});
```

- [ ] **Step 2: 통과를 확인한다**

Run: `npx playwright test e2e/table-grid.spec.ts`
Expected: PASS — 1 passed

- [ ] **Step 3: 이 테스트가 회귀를 실제로 잡는지 증명한다(음성 대조)**

`lib/editor/table-nodes.ts`에서 `{ class: "tbl" }` 주입을 잠시 제거해 2026-07-27 버그를 재현한다.

Run: `npx playwright test e2e/table-grid.spec.ts`
Expected: **FAIL** — `toHaveClass(/\btbl\b/)` 에서 실패

확인 후 `table-nodes.ts`를 **반드시 원복**하고 다시 실행해 PASS 확인.

- [ ] **Step 4: 게이트 후 커밋**

```bash
npm test && npm run build && npx playwright test e2e/table-grid.spec.ts
```

```bash
git add e2e/table-grid.spec.ts
git commit -m "test(e2e): 표 격자 스모크 — 라이브 DOM의 .tbl 클래스와 계산된 border 단언

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 스모크 #2 — 상단 툴바 한 줄 (+ 임계값 실측·스펙 역기입)

**Files:**
- Create: `e2e/toolbar-layout.spec.ts`
- Modify: `docs/superpowers/specs/2026-07-27-e2e-smoke-design.md` (§5.3 역기입 — **DoD 항목**)

**Interfaces:**
- Consumes: `openHarness`, `test`, `expect` (Task 3).

**이 태스크만 순서가 다르다:** 스펙 §5.3이 임계값을 의도적으로 비워 두었다. 추측 임계는 상시
flaky이거나 아무것도 안 잡는다. **먼저 재고, 그 값으로 임계를 정하고, 측정치와 근거를 스펙에
되적는다.**

- [ ] **Step 1: 정상 상태를 실측한다**

측정 전용 스펙을 임시로 만든다 — `e2e/__measure.spec.ts`:

```ts
import { openHarness, test } from "./fixtures";

test("측정: 툴바 버튼 y좌표 분포와 툴바 높이", async ({ page }) => {
  await openHarness(page);
  const toolbar = page.locator(".top-toolbar");
  const bar = await toolbar.boundingBox();
  const ys: number[] = [];
  const buttons = toolbar.locator("button");
  for (let i = 0; i < (await buttons.count()); i++) {
    const b = await buttons.nth(i).boundingBox();
    if (b) ys.push(b.y);
  }
  console.log("버튼 수:", ys.length);
  console.log("y 최소/최대/차이:", Math.min(...ys), Math.max(...ys), Math.max(...ys) - Math.min(...ys));
  console.log("툴바 높이:", bar?.height);
});
```

Run: `npx playwright test e2e/__measure.spec.ts --reporter=list`
측정값 3개(버튼 수 / y 차이 / 툴바 높이)를 받아 적는다.

- [ ] **Step 2: 임계값을 정하고 측정 스펙을 지운다**

- **y 차이 상한** = 실측 y 차이 + 여유. 한 줄이면 실측 차이는 0에 가깝다. 세로로 무너지면
  버튼 높이(약 28~32px)만큼 벌어지므로, **상한은 실측값과 버튼 높이의 중간**으로 잡는다
  (예: 실측 0 → 상한 8). 붕괴는 확실히 잡고 서브픽셀 흔들림은 안 잡는 폭이다.
- **툴바 높이 상한** = 실측 높이 + 여유. 두 줄이 되면 대략 2배가 되므로 **실측 × 1.5**로 잡는다.

```bash
rm e2e/__measure.spec.ts
```

- [ ] **Step 3: 테스트를 쓴다** (아래 `TODO_실측` 두 곳을 Step 2에서 정한 숫자로 바꾼다)

Create `e2e/toolbar-layout.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 2026-07-23 회귀 직격 — 상단 툴바 세로 붕괴.
 *
 * flex 레이아웃 계산은 jsdom에 없다. 여기서는 진짜 브라우저가 배치한 좌표를 본다.
 *
 * 임계값 근거는 스펙 §5.3에 역기입돼 있다(실측값 + 여유).
 * 붕괴하면 버튼들이 서로 다른 행으로 내려가 y 차이가 버튼 높이만큼 벌어지고,
 * 툴바 자체 높이도 배로 늘어난다 — 두 신호를 모두 본다.
 */
const MAX_Y_SPREAD = TODO_실측; // Step 2에서 정한 y 차이 상한
const MAX_TOOLBAR_HEIGHT = TODO_실측; // Step 2에서 정한 툴바 높이 상한

test("상단 툴바 버튼이 한 줄에 있고 툴바가 한 줄 높이를 넘지 않는다", async ({ page }) => {
  await openHarness(page);

  const toolbar = page.locator(".top-toolbar");
  await expect(toolbar).toBeVisible();

  const buttons = toolbar.locator("button");
  const count = await buttons.count();
  expect(count).toBeGreaterThan(5); // 툴바가 비어 있으면 아래 단언이 무의미해진다

  const ys: number[] = [];
  for (let i = 0; i < count; i++) {
    const box = await buttons.nth(i).boundingBox();
    expect(box, `버튼 ${i}가 보이지 않는다`).not.toBeNull();
    ys.push(box!.y);
  }

  const spread = Math.max(...ys) - Math.min(...ys);
  expect(spread, "툴바 버튼이 여러 줄로 흩어졌다").toBeLessThanOrEqual(MAX_Y_SPREAD);

  const bar = await toolbar.boundingBox();
  expect(bar).not.toBeNull();
  expect(bar!.height, "툴바가 한 줄 높이를 넘었다").toBeLessThanOrEqual(MAX_TOOLBAR_HEIGHT);
});
```

- [ ] **Step 4: 통과를 확인한다**

Run: `npx playwright test e2e/toolbar-layout.spec.ts`
Expected: PASS — 1 passed

- [ ] **Step 5: 스펙에 역기입한다 (DoD 항목 — 생략 금지)**

Modify `docs/superpowers/specs/2026-07-27-e2e-smoke-design.md` §5.3의 "역기입 의무" 인용
블록 바로 아래에, 실측 결과를 아래 형식으로 추가한다:

```markdown
> **확정(구현 시 실측):** 버튼 N개, y 최소 A / 최대 B / 차이 C px, 툴바 높이 H px.
> 채택: `MAX_Y_SPREAD = X`(붕괴 시 버튼 높이 ~28-32px만큼 벌어지므로 그 절반 아래),
> `MAX_TOOLBAR_HEIGHT = Y`(실측 H의 1.5배 — 두 줄이 되면 약 2배가 된다).
```

- [ ] **Step 6: 게이트 후 커밋**

```bash
npm test && npm run build && npx playwright test e2e/toolbar-layout.spec.ts
```

```bash
git add e2e/toolbar-layout.spec.ts docs/superpowers/specs/2026-07-27-e2e-smoke-design.md
git commit -m "test(e2e): 상단 툴바 한 줄 스모크 — 임계값 실측 확정 후 스펙 역기입

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: 스모크 #3 — 드래그 핸들 메뉴

**Files:**
- Create: `e2e/handle-menu.spec.ts`

**Interfaces:**
- Consumes: `openHarness`, `test`, `expect` (Task 3).

**셀렉터 출처:** `.handle-btn`(DragHandle.tsx:255), `.handle-menu[role="menu"]`(DragHandle.tsx:198),
메뉴 항목 "복제"·"삭제"(DragHandle.tsx:149-150).

- [ ] **Step 1: 테스트를 쓴다**

Create `e2e/handle-menu.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 2026-07-23 회귀 직격 — 핸들 메뉴 클릭 시 크래시.
 *
 * 검증 규칙 1(누적): "모든 팝오버·메뉴·드롭다운은 열기 → 항목 클릭 → 닫기까지 실제로
 * 조작해 확인한다. 렌더 확인 ≠ 동작 확인." 그때 메뉴를 열어보지 않아 크래시를 배포까지
 * 통과시켰다.
 *
 * pageerror 그물(fixtures.ts)이 자동으로 걸려 있어, 조작 중 미처리 예외가 하나라도
 * 나면 이 테스트가 실패한다 — 그것이 이 테스트의 핵심 방어선이다.
 */
test("핸들 메뉴를 열고 항목을 클릭해도 크래시하지 않는다", async ({ page }) => {
  await openHarness(page);

  await page.locator(".detail-content").click();
  await page.keyboard.type("핸들 대상 블록");

  const paragraph = page.locator(".detail-content p").first();
  await expect(paragraph).toContainText("핸들 대상 블록");

  // 핸들은 블록 hover 시에만 나타난다.
  await paragraph.hover();
  const handle = page.locator(".handle-btn");
  await expect(handle).toBeVisible();

  await handle.click();
  const menu = page.locator(".handle-menu");
  await expect(menu).toBeVisible();

  // 열기 → 항목 클릭 → 결과까지 본다.
  await menu.getByText("복제", { exact: true }).click();
  await expect(menu).toBeHidden();
  await expect(page.locator(".detail-content p")).toHaveCount(2);
});
```

- [ ] **Step 2: 통과를 확인한다**

Run: `npx playwright test e2e/handle-menu.spec.ts`
Expected: PASS — 1 passed

- [ ] **Step 3: 게이트 후 커밋**

```bash
npm test && npm run build && npx playwright test e2e/handle-menu.spec.ts
```

```bash
git add e2e/handle-menu.spec.ts
git commit -m "test(e2e): 핸들 메뉴 스모크 — 열기·항목 클릭·pageerror 0 검증

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 스모크 #4 — 색 팝오버

**Files:**
- Create: `e2e/color-popover.spec.ts`

**Interfaces:**
- Consumes: `openHarness`, `test`, `expect` (Task 3).

**셀렉터·값 출처:** 버튼 `aria-label="글자색"`(TopToolbar.tsx:218), 팝오버 `.clr-pop`
(ColorPopover.tsx:23), 스와치 `aria-label`은 팔레트 `label`(ColorPopover.tsx:29) — 빨강 =
`#e5484d` = `rgb(229, 72, 77)`(palette.ts:14). 닫힘은 팝오버 래퍼 `onClick`이
`setColorOpen(null)`을 호출하며, 버튼에 `aria-expanded`가 반영된다(TopToolbar.tsx:219, 228-230).

**주의:** 텍스트를 선택하면 `FormatToolbar`(`.fmt-bar`)도 함께 뜬다. 두 툴바에 겹치는
`aria-label`이 있으므로(`링크`) 버튼 조회는 반드시 `.top-toolbar`로 스코프한다.

- [ ] **Step 1: 테스트를 쓴다**

Create `e2e/color-popover.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 색 팝오버 — 열기 → 스와치 클릭 → 적용 → 닫기까지 실제로 조작한다(검증 규칙 1).
 * floating-ui의 실좌표는 jsdom에서 계산되지 않으므로 뷰포트 안에 있는지도 함께 본다.
 */
test("색 팝오버를 열고 스와치를 눌러 글자색이 적용되고 닫힌다", async ({ page }) => {
  await openHarness(page);

  await page.locator(".detail-content").click();
  await page.keyboard.type("색 대상 텍스트");
  await page.keyboard.press("ControlOrMeta+a");

  // 선택 시 뜨는 .fmt-bar와 라벨이 겹칠 수 있어 상단 툴바로 스코프한다.
  const colorButton = page.locator(".top-toolbar").getByRole("button", { name: "글자색" });
  await colorButton.click();

  const popover = page.locator(".clr-pop");
  await expect(popover).toBeVisible();
  await expect(colorButton).toHaveAttribute("aria-expanded", "true");

  // 뷰포트 안에 있는가(floating-ui 실좌표).
  const box = await popover.boundingBox();
  const viewport = page.viewportSize();
  expect(box).not.toBeNull();
  expect(viewport).not.toBeNull();
  expect(box!.x).toBeGreaterThanOrEqual(0);
  expect(box!.y).toBeGreaterThanOrEqual(0);
  expect(box!.x + box!.width).toBeLessThanOrEqual(viewport!.width);
  expect(box!.y + box!.height).toBeLessThanOrEqual(viewport!.height);

  await popover.getByRole("button", { name: "빨강" }).click();

  // 적용 — Color 확장은 인라인 style로 직렬화된다(palette.ts:14, #e5484d).
  const colored = page.locator('.detail-content span[style*="color"]').first();
  await expect(colored).toHaveCSS("color", "rgb(229, 72, 77)");

  // 닫힘
  await expect(popover).toBeHidden();
  await expect(colorButton).toHaveAttribute("aria-expanded", "false");
});
```

- [ ] **Step 2: 통과를 확인한다**

Run: `npx playwright test e2e/color-popover.spec.ts`
Expected: PASS — 1 passed

- [ ] **Step 3: 게이트 후 커밋**

```bash
npm test && npm run build && npx playwright test e2e/color-popover.spec.ts
```

```bash
git add e2e/color-popover.spec.ts
git commit -m "test(e2e): 색 팝오버 스모크 — 열기·적용·닫기 + 뷰포트 내 위치 검증

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: 스모크 #5 — 표 셀 플로팅 툴바

**Files:**
- Create: `e2e/table-toolbar.spec.ts`

**Interfaces:**
- Consumes: `openHarness`, `test`, `expect` (Task 3).

**셀렉터 출처:** `.tbl-toolbar[role="toolbar"][aria-label="표 편집"]`(TableToolbar.tsx:41),
액션 `aria-label`은 `title`과 동일 — "행 아래 삽입" 등(TableToolbar.tsx:26-32, 47-48).

- [ ] **Step 1: 테스트를 쓴다**

Create `e2e/table-toolbar.spec.ts`:

```ts
import { expect, openHarness, test } from "./fixtures";

/**
 * 표 셀에 커서가 있을 때 뜨는 플로팅 툴바. FormatToolbar의 BubbleMenu 패턴을 재사용하므로
 * 실좌표 배치가 jsdom에서 검증되지 않는다 — 뷰포트 안에 뜨는지와 실제 동작을 함께 본다.
 */
test("셀을 클릭하면 표 툴바가 뜨고 행 삽입이 동작한다", async ({ page }) => {
  await openHarness(page);

  await page.locator(".detail-content").click();
  await page.locator(".top-toolbar").getByRole("button", { name: "표 삽입" }).click();

  const table = page.locator(".detail-content table");
  await expect(table).toBeVisible();
  await expect(table.locator("tr")).toHaveCount(3); // 헤더 1 + 본문 2

  await table.locator("td").first().click();

  const toolbar = page.locator(".tbl-toolbar");
  await expect(toolbar).toBeVisible();

  const box = await toolbar.boundingBox();
  const viewport = page.viewportSize();
  expect(box).not.toBeNull();
  expect(viewport).not.toBeNull();
  expect(box!.x).toBeGreaterThanOrEqual(0);
  expect(box!.y).toBeGreaterThanOrEqual(0);
  expect(box!.x + box!.width).toBeLessThanOrEqual(viewport!.width);
  expect(box!.y + box!.height).toBeLessThanOrEqual(viewport!.height);

  await toolbar.getByRole("button", { name: "행 아래 삽입" }).click();
  await expect(table.locator("tr")).toHaveCount(4);
});
```

- [ ] **Step 2: 통과를 확인한다**

Run: `npx playwright test e2e/table-toolbar.spec.ts`
Expected: PASS — 1 passed

- [ ] **Step 3: 전체 스모크가 함께 도는지 확인한다**

Run: `npm run test:e2e`
Expected: **7 passed** (harness 2 + 스모크 5)

- [ ] **Step 4: 게이트 후 커밋**

```bash
npm test && npm run build && npm run test:e2e
```

```bash
git add e2e/table-toolbar.spec.ts
git commit -m "test(e2e): 표 플로팅 툴바 스모크 — 등장·뷰포트 내 위치·행 삽입 동작

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 9: CI 편입

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `npm run verify:harness`(Task 2), `npm run test:e2e`(Task 3~8).

**배치 원칙:** 빠른 게이트를 먼저 둔다 — `npm test`(6초) → `npm run build` → `verify:harness` →
`test:e2e`. 그리고 **로컬에서만 도는 검증은 결국 안 돈다**는 이유로 `verify:harness`도 CI에 넣는다.

- [ ] **Step 1: 워크플로에 스텝을 추가한다**

Modify `.github/workflows/ci.yml` — 기존 "Build" 스텝 **뒤에** 아래를 추가한다:

```yaml
      - name: Verify harness excluded from production bundle
        # 하니스가 프로덕션 번들에 새지 않는지. 플래그 없이 빌드해 라우트·마커 부재를 본다.
        # 로컬에서만 도는 검증은 결국 아무도 안 돌리므로 CI에 건다.
        run: npm run verify:harness
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ vars.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ vars.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install Playwright chromium
        run: npx playwright install --with-deps chromium

      - name: E2E smoke (playwright)
        # jsdom이 못 잡는 렌더 회귀 5건. webServer가 NEXT_PUBLIC_E2E=1로 dev 서버를 띄운다.
        run: npm run test:e2e
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ vars.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ vars.NEXT_PUBLIC_SUPABASE_ANON_KEY }}

      - name: Upload playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
```

- [ ] **Step 2: 로컬에서 CI와 같은 순서로 전부 돌린다**

```bash
npm ci && npm test && npm run build && npm run verify:harness && npm run test:e2e
```
Expected: 523 테스트 통과 · 빌드 성공 · `✓ 하니스 미유출 확인` · **7 passed**

- [ ] **Step 3: 커밋하고 푸시해 CI 결과를 확인한다**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: E2E 스모크와 하니스 유출 검증을 게이트에 편입

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
git push -u origin 006-e2e-smoke
```

Run: `gh run watch`
Expected: CI **success**. 실패 시 `gh run view --log-failed`로 원인을 확인한다.

- [ ] **Step 4: DoD 최종 확인**

스펙 §9의 5항목을 하나씩 확인한다:

1. `npm test` — 523 통과 (기존 520 + 게이트 3)
2. `npm run verify:harness` — 마커 0건 + 라우트 부재
3. `npm run test:e2e` — 7 passed, pageerror 0. **테스트 #2 임계값 실측 확정 + 스펙 §5.3 역기입 완료**(Task 5 Step 5)
4. 위 3종이 CI에서도 초록
5. 프로덕션 404 실측 — main 머지·배포 후:
   ```bash
   curl -s -o /dev/null -w "%{http_code}\n" https://ium.ai.kr/e2e-harness/editor
   ```
   Expected: `404`

---

## 잔여 리스크 (스펙 §8에서 이월)

- **정적 대역의 표류** — `.detail-breadcrumb`·`.detail-cover`는 하니스에서 정적 마크업이라
  원본이 바뀌어도 따라가지 않는다. 실제 경로 검증은 Supabase 목킹 도입 시 가능(BACKLOG).
- **CI 시간 +1~2분** — `actions/cache`로 브라우저 캐시, chromium 단일로 완화.
- **`page.e2e.tsx`의 타입 검사** — `pageExtensions`에서 빠져도 `tsconfig.json`의 include에
  걸려 `next build`의 TypeScript 단계가 검사한다. Task 1 Step 7의 플래그 없는 빌드가
  이를 확인해 준다(빌드가 성공하면 타입도 통과한 것).
